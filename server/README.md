# VCPKG Manifest - Server

## Available Scripts

In the project directory, you can run:

### `npm run build`

Compiles the code in JavaScript inside a dist directory.

### `npm run dev`

Runs the Node.js server in development mode.
